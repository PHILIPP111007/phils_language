import "./module.p"


def main() -> int:
    var a: int = 10
    var b: int = 100
    
    var c: int = 0
    c = mul(a, b)
    # var x: int = dd
    print(c)

    var s: str = 10 * "asff"

    var x: int = 0
    x = 1 + 1 / 10

    var xx: dict = {}


    var p: *int = &x
    *p = 10
    print(p)

    return 1 * a
